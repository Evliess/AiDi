<!--pages/add-vip/add-vip.wxml-->
<navigation-bar></navigation-bar>
<view class="container"   scroll-y type="custom">
  <view class="title">
    <view>AIDI 会员划卡</view>
  </view>

  <view class="card">
    <view class="card-header">
      <p class="card-title">会员划卡</p>
    </view>
    <view class="form-group">
      <label class="form-label" for="phone">会员号或昵称</label>
      <input type="text" class="form-input" placeholder="请输入会员号或昵称" value="{{user.phone}}"bindinput="onPhoneChange"></input>
    </view>
    <user-info wx:if="{{user.name}}" jsonData="{{user}}"></user-info>
    <view class="card-buttons">
      <button class="btn btn-outline" style="visibility: hidden;">重置</button>
      <button class="btn btn-primary" bind:tap="find">查找</button>
    </view>
  </view>

  <view class="card">
    <view class="card-header">
      <p class="card-title">划卡</p>
    </view>
    <view wx:if="{{user.oldType=='day'}}" class="form-group">
      <label class="form-label" for="money">设置剩余次数</label>
      <input type="number" class="form-input" placeholder="请设置剩余次数" bindinput="onLeftCountChange" value="{{user.leftCount}}"></input>
    </view>
    <view class="form-group">
      <checkbox-group bindchange="onRadioChange">
        <label class="form-label" for="type-1">划卡项目</label>
        <label wx:for="{{items}}" wx:key="value">
          <checkbox value="{{item.value}}" checked="{{item.checked}}" /> {{item.name}}
        </label>
      </checkbox-group>
    </view>
    <view class="card-buttons">
      <button class="btn btn-outline" style="visibility: hidden;">重置</button>
      <button class="btn btn-primary" bind:tap="play">划卡</button>
    </view>
  </view>

  <view class="card">
    <view class="card-header">
      <p class="card-title">积分</p>
    </view>
    <view class="form-group">
      <label class="form-label" for="money">设置积分</label>
      <input type="number" class="form-input" placeholder="请设置积分" bindinput="onScoreChange" value="{{user.score}}"></input>
    </view>
    <view class="card-buttons">
      <button class="btn btn-outline" style="visibility: hidden;">重置</button>
      <button class="btn btn-primary" bind:tap="score">修改积分</button>
    </view>
  </view>

</view>